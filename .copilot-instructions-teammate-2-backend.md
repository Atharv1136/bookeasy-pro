# GitHub Copilot Custom Instructions - Teammate 2 (Backend)

## Role & Context
You are assisting **[REPLACE_WITH_YOUR_NAME]** who is contributing to the Appointly hackathon project as the **Backend Developer**.

## Your Responsibilities
- Implement server-side functions and APIs
- Handle authentication and authorization
- Manage database operations
- Handle email communications
- Process payments with Razorpay
- Create API routes and middleware

## Project Structure (Your Files)
```
src/
  ├── server/
  │   ├── auth.server.ts (Authentication setup & middleware)
  │   ├── auth.functions.ts (Auth logic - login, signup, OTP)
  │   ├── db.server.ts (Database connection & queries)
  │   ├── email.server.ts (Email sending service)
  │   ├── payments.functions.ts (Razorpay integration)
  │   ├── admin.functions.ts (Admin-specific operations)
  │   ├── organiser.functions.ts (Organizer-specific operations)
  │   ├── services.functions.ts (Services management)
  │   └── bookings.functions.ts (Booking management)
  ├── lib/
  │   ├── auth-context.tsx (React context - YOU handle auth logic)
  │   └── types.ts (TypeScript interfaces)
  
wrangler.jsonc (Cloudflare Workers config)
```

## Tech Stack
- **Runtime:** Cloudflare Workers / Node.js
- **Language:** TypeScript
- **Auth:** Session-based authentication
- **Database:** (Connected via `db.server.ts`)
- **Email:** (Via `email.server.ts`)
- **Payments:** Razorpay API
- **Frontend Framework:** React (just for auth-context.tsx)

## Key Functions & Endpoints

### Authentication
- `auth.functions.ts` - signup, login, logout, verify-otp
- `auth.server.ts` - session management, middleware

### User Management
- `admin.functions.ts` - user management, permissions
- `organiser.functions.ts` - organizer profile, settings
- `services.functions.ts` - manage services catalog

### Bookings & Appointments
- `bookings.functions.ts` - create, update, cancel bookings
- `appointments.tsx` - appointment-related operations

### Payments
- `payments.functions.ts` - Razorpay integration, payment processing

### Email & Notifications
- `email.server.ts` - send emails for confirmations, OTP, etc.

## Key Guidelines
1. ✅ Use TypeScript for all server functions
2. ✅ Handle errors properly with try-catch
3. ✅ Validate all user inputs
4. ✅ Use environment variables for secrets (API keys, DB credentials)
5. ✅ Implement proper authentication checks
6. ✅ Log important operations
7. ✅ Return consistent API response formats
8. ❌ Don't expose sensitive data
9. ❌ Don't modify frontend components (`src/components/`)
10. ❌ Don't modify routing files (`src/routes/`)

## When I Ask For Help
When you help me:
- Focus on **server-side logic and APIs**
- Help with authentication flows
- Suggest database query optimizations
- Debug payment integration issues
- Review error handling and validation
- Help with email template logic

## About My Teammates
- **Teammate 1:** Handles frontend (UI components, pages, routing)
- **Teammate 3:** Handles config (dependencies, build setup, Wrangler)

## Git Workflow
- **Branch:** `feature/[YOUR_NAME]-backend`
- **Files to commit:** Everything in my section above
- **Commit message:** `feat: Add backend authentication and server functions`
- **Don't commit:** Frontend files, UI components, routes

## Typical API Patterns in This Project
```typescript
// Example server function
export async function loginUser(email: string, password: string) {
  try {
    // Validate input
    if (!email || !password) {
      throw new Error("Email and password required");
    }
    
    // Database query
    const user = await db.users.findByEmail(email);
    
    // Authentication logic
    if (!user || !verifyPassword(password, user.password)) {
      throw new Error("Invalid credentials");
    }
    
    // Session creation
    const session = await createSession(user);
    
    // Return response
    return {
      success: true,
      session,
      user: { id: user.id, email: user.email, role: user.role }
    };
  } catch (error) {
    return {
      success: false,
      error: error.message
    };
  }
}
```

## Common Tasks I Might Ask For
- "Create a new API endpoint for..."
- "Add validation for user input"
- "Set up email notification for..."
- "Handle Razorpay webhook"
- "Add role-based access control"
- "Optimize this database query"

## Helpful Commands for My Workflow
```bash
# Check my files
git status

# See what I'm changing
git diff

# Commit my work
git add src/server/ src/lib/auth-context.tsx wrangler.jsonc
git commit -m "feat: [YOUR_FEATURE_DESCRIPTION]"

# Push to GitHub
git push -u origin feature/[YOUR_NAME]-backend

# Test locally
npm run dev
# (or bun run dev, depending on setup)
```

## Environment Variables (Examples)
```
DATABASE_URL=your_database_connection_string
JWT_SECRET=your_secret_key
RAZORPAY_KEY_ID=your_razorpay_key
RAZORPAY_KEY_SECRET=your_razorpay_secret
EMAIL_SERVICE_KEY=your_email_service_key
CLOUDFLARE_ACCOUNT_ID=your_cf_account_id
```

## Resources
- Cloudflare Workers: https://workers.cloudflare.com/
- Wrangler CLI: https://developers.cloudflare.com/workers/wrangler/
- TypeScript: https://www.typescriptlang.org/
- Razorpay API: https://razorpay.com/docs/
- Node.js Auth: https://nodejs.org/en/knowledge/file-system/security/introduction/

## Response Format Standard
```typescript
// Success response
{
  success: true,
  data: { ... },
  message: "Operation successful"
}

// Error response
{
  success: false,
  error: "Error message",
  code: "ERROR_CODE"
}
```

---

**Customize this prompt:**
- Replace `[REPLACE_WITH_YOUR_NAME]` with your actual name
- Replace `[YOUR_NAME]` in git commands with your name
- Save as `.copilot-instructions-backend.md` in your local repo

**Usage in VS Code:**
1. Go to Settings (Cmd/Ctrl + ,)
2. Search "Copilot"
3. Paste this into "Copilot: Custom Instructions"
