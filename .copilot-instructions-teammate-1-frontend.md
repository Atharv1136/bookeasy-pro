            # GitHub Copilot Custom Instructions - Teammate 1 (Frontend)

## Role & Context
You are assisting **Atharv** who is contributing to the Appointly hackathon project as the **Frontend Developer**.

## Your Responsibilities
- Build and maintain UI components in `src/components/ui/`
- Create route structures and page components in `src/routes/`
- Configure routing with TanStack Router
- Set up Vite configuration
- Implement responsive designs using shadcn/ui

## Project Structure (Your Files)
```
src/
  ├── components/
  │   ├── layout.tsx (Main layout wrapper)
  │   ├── role-guard.tsx (Authentication guard)
  │   └── ui/ (All UI components - accordion, alert, badge, button, etc.)
  ├── routes/
  │   ├── __root.tsx (Root layout)
  │   ├── index.tsx (Home page)
  │   ├── login.tsx (Login page)
  │   ├── signup.tsx (Signup page)
  │   ├── appointments.tsx (Appointments listing)
  │   ├── services.tsx (Services listing)
  │   ├── profile.tsx (User profile)
  │   ├── book.$id.tsx (Booking page)
  │   ├── organiser.* (Organizer routes)
  │   └── admin.* (Admin routes)
  ├── router.tsx (Router configuration)
  ├── routeTree.gen.ts (Generated route tree)
  ├── assets/ (Images, icons, static files)
  └── styles.css (Global styles)

vite.config.ts (Your config file)
components.json (shadcn/ui config)
```

## Tech Stack
- **Framework:** React 18+
- **Router:** TanStack Router (TKR)
- **UI Library:** shadcn/ui
- **Build Tool:** Vite
- **Language:** TypeScript
- **Styling:** Tailwind CSS

## Key Guidelines
1. ✅ Use shadcn/ui components from `src/components/ui/`
2. ✅ Follow existing component patterns and naming
3. ✅ Keep components modular and reusable
4. ✅ Use TypeScript for type safety
5. ✅ Mobile-first responsive design
6. ✅ Use TanStack Router for navigation
7. ❌ Don't modify backend files (`src/server/`)
8. ❌ Don't modify config files (unless instructed)

## When I Ask For Help
When you help me:
- Focus only on **frontend/UI components**
- Suggest improvements to my route structure
- Help debug React component issues
- Optimize Vite configuration for frontend
- Review shadcn/ui integration

## About My Teammates
- **Teammate 2:** Handles backend (`src/server/`, auth, API)
- **Teammate 3:** Handles config (dependencies, build setup)

## Git Workflow
- **Branch:** `feature/atharv-frontend`
- **Files to commit:** Everything in my section above
- **Commit message:** `feat: Add frontend components and routing`
- **Don't commit:** Backend files, config files

## Questions to Ask Me
Before you suggest major changes:
1. "What specific component are you building?"
2. "What's the current issue you're facing?"
3. "Do you want responsive/mobile versions?"
4. "Should this use existing UI components or custom?"

## Helpful Commands for My Workflow
```bash
# Check my files
git status

# See what I'm changing
git diff

# Commit my work
git add src/components/ src/routes/ src/assets/ vite.config.ts components.json
git commit -m "feat: [YOUR_COMPONENT_DESCRIPTION]"

# Push to GitHub
git push -u origin feature/atharv-frontend
```

## Resources
- shadcn/ui docs: https://ui.shadcn.com/
- TanStack Router: https://tanstack.com/router/latest
- Vite docs: https://vitejs.dev/
- Tailwind CSS: https://tailwindcss.com/

---

**Customize this prompt:**
- ✅ Already customized with name: **Atharv**
- ✅ Git branch: `feature/atharv-frontend`
- Ready to use!

**Usage in VS Code:**
1. Go to Settings (Cmd/Ctrl + ,)
2. Search "Copilot"
3. Paste this into "Copilot: Custom Instructions"
